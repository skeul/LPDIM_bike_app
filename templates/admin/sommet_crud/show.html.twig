{% extends 'base.html.twig' %}

{% block title %}Sommet{% endblock %}

{% block body %}
    <h1>Sommet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sommet.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sommet.nom }}</td>
            </tr>
            <tr>
                <th>Altitude</th>
                <td>{{ sommet.altitude }}</td>
            </tr>
            <tr>
                <th>Lieu</th>
                <td>{{ sommet.lieu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sommet_crud_index') }}">back to list</a>

    <a href="{{ path('sommet_crud_edit', {'id': sommet.id}) }}">edit</a>

    {{ include('admin/sommet_crud/_delete_form.html.twig') }}
{% endblock %}
