{% extends 'base.html.twig' %}

{% block title %}Parcours{% endblock %}

{% block body %}
    <h1>Parcours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parcour.id }}</td>
            </tr>
            <tr>
                <th>Distance</th>
                <td>{{ parcour.distance }}</td>
            </tr>
            <tr>
                <th>Difficulte</th>
                <td>{{ parcour.difficulte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parcours_crud_index') }}">back to list</a>

    <a href="{{ path('parcours_crud_edit', {'id': parcour.id}) }}">edit</a>

    {{ include('admin/parcours_crud/_delete_form.html.twig') }}
{% endblock %}
