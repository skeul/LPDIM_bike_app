<form method="post" action="{{ path('vtt_delete', {'id': vtt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vtt.id) }}">
    <button class="btn">Delete</button>
</form>
